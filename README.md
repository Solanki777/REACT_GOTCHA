npx create-react-app namste-world
npm start
jsx - Java Script Xml
Hot reloading - As change website as you change the code
cc - Class Component
rfc = Functional component
imr - import react
props - pass data or properties to other components in read mode only


- function inside other function is created as arrow function . Normal function inside other function where other functin have to use this for parent function use

- when you have to use run the function with some values and disply the values as per change so you have to use arrow function here we use for increment and decrement like this 
<button type="button" class="btn btn-primary btn-sm"  onClick={
            () => {props.icr_quantity(props.idx)}
          }>